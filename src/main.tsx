import React from 'react';
import { render } from 'react-dom';
import App from './components/App';

// render #roots in index.html and connects the React App
render(<App />, document.getElementById('root'));
